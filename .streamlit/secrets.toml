# ==========================================================
# CogniX — Streamlit Secrets (LOCAL TEMPLATE)
# ==========================================================
# DO NOT commit this file to GitHub.
# This file is already in .gitignore.
#
# For Streamlit Cloud deployment:
#   App Settings → Secrets → paste contents here
# ==========================================================

# --- Gemini AI ---
GEMINI_API_KEY = "your_gemini_api_key_here"
GEMINI_MODEL   = "models/gemini-flash-latest"

# --- Gmail SMTP (for OTP emails) ---
GMAIL_USER = "your_gmail@gmail.com"
GMAIL_PASS = "your_gmail_app_password_here"

# --- Super Admin Seed (first-run only) ---
# Change these before first deployment.
# After the admin account is created, these are no longer used.
ADMIN_USERNAME = "admin"
ADMIN_NAME     = "Admin"
ADMIN_EMAIL    = "admin@cognix.app"
ADMIN_PASSWORD = "ChangeMe@2026!"
